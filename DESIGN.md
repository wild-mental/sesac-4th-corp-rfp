# MAKJI 브레드마켓 — 프레젠테이션 디자인 정책

> **단일 진실 원천(SSOT).** 기준 레퍼런스는 `MAKJI_UX소개_슬라이드이미지/` 의 4장(서비스 경험 01~03, 관리자 경험 04).
> **지수 명칭도 이 레퍼런스를 따른다** — 통밀 브레드 지수 / 크루아상 지수 / 골든 브레드 지수. 구 명칭(마켓·글로벌·골드 브랜드 지수)은 폐기.
> 이 문서와 어긋나는 스타일이 발견되면 이 문서가 우선한다. 적용 대상: `index.html`.

---

## 1. 원칙

| # | 원칙 | 의미 |
|---|------|------|
| 1 | **깨끗한 화이트가 바탕** | 캔버스는 순백(`#FFFFFF`). 배경에 색을 깔지 않는다. 면 분할은 **테두리 + 아주 옅은 블루 틴트**로만 한다. |
| 2 | **블루가 주인공** | MAKJI 로고의 블루 계열이 유일한 주색. 강조·CTA·활성 상태·번호·아이콘은 모두 블루. |
| 3 | **설명문은 짙게** | 본문·설명 문구는 흐린 회색이 아니라 **짙은 슬레이트**(`--ink-2`)로. 발표장 뒤에서도 읽힌다. |
| 4 | **강조는 3단으로** | 제목·리드문 강조는 **블루**(헤딩 계열), 본문 기본 강조는 **잉크 굵게**, 특별히 튀어야 할 핵심어만 **앰버**. 블루 면 위에서는 `--on-brand-accent`. 같은 층위에 같은 색을 겹쳐 쓰지 않는다. |
| 5 | **의미색은 3개뿐** | 성공/적중=그린, 하락/미적중=레드, 리워드·TOP=앰버. 그 외 장식적 색 사용 금지. |
| 6 | **여백이 곧 품질** | 카드 내부 14~18px, 카드 사이 12~16px, 슬라이드 좌우 56px를 지킨다. |

---

## 2. 컬러 토큰

CSS 변수는 `index.html`의 `:root`에 정의한다. **하드코딩 금지 — 반드시 토큰을 쓴다.**

### 바탕 · 글자

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--bg` | `#FFFFFF` | 슬라이드 캔버스 |
| `--paper` | `#FFFFFF` | 카드 면 |
| `--paper-2` | `#F4F8FE` | 이미지 자리·비활성 면 |
| `--ink` | `#101418` | 제목·카드 타이틀 (거의 검정) |
| `--ink-2` | `#42525F` | **본문·설명 문구** (짙은 슬레이트) |
| `--ink-3` | `#64748B` | 라벨·캡션·보조 (흰 배경 대비 4.8:1 — AA 충족) |
| `--line` | `rgba(16,42,74,.11)` | 카드 테두리 |
| `--line-2` | `rgba(16,42,74,.055)` | 표 내부 구분선 |

### 브랜드 블루

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--forest` | `#1B5FD9` | **주색.** CTA, 활성 챕터, 진행바, 표 헤더, 제목 강조 |
| `--forest-2` | `#3B82E6` | 보조 블루. 불릿, 2차 헤더, 아이콘 |
| `--sage` | `#E4EFFC` | 블루 틴트 면 (강) |
| `--sage-2` | `#F2F7FE` | 블루 틴트 면 (약) — 칩·라벨 바탕 |
| `--blue` | `#3F8FD0` | 로고 스크립트 블루. 브랜드마크 점 |
| `--on-brand` | `#9BC8EE` | **블루 면 위** 보조 강조 (정확도 산식, 핵심 지표) |
| `--on-brand-accent` | `#FFD98A` | **블루 면 위** 핵심 강조 — 흰 글자 위에서 튀어야 할 때 |

> 토큰 이름(`--forest`/`--sage`)은 이전 테마의 잔재지만 **의도적으로 유지**한다.
> 12장 전체가 이 이름을 참조하므로, 이름을 바꾸면 전면 수정이 필요하고 회귀 위험만 커진다. 값만 블루로 재정의한다.

### 강조(이색) · 의미색

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--gold` (=accent) | `#C4841F` | **문장 안 핵심어 강조**, 리워드·TOP 라벨, eyebrow |
| `--gold-2` | `#E0A83C` | 강조 룰(rule), 좌측 보더, 타임라인 현재 지점 |
| `--gold-soft` | `#FBF1DC` | 앰버 칩 바탕 |
| `--up` | `#2E9E5B` | 적중·상승·완료 |
| `--down` | `#E0463F` | 미적중·하락 |

### 지수 3종 식별색

| 지수 | 토큰 | 값 | 근거 |
|------|------|-----|------|
| 통밀 브레드 지수 (KOSPI) | `--idx-market` | `#1B5FD9` | 주색 — 시장 지수의 기준 |
| 크루아상 지수 (환율) | `--idx-global` | `#2E9E5B` | 그린 |
| 골든 브레드 지수 (금 시세) | `--idx-gold` | `#C4841F` | 앰버 = 금 |

---

## 3. 타이포그래피

폰트: `Pretendard` → `Apple SD Gothic Neo` → `Noto Sans KR` 폴백. CDN 실패해도 시스템 폰트로 무너지지 않아야 한다.

| 역할 | 크기 | 굵기 | 자간 | 색 |
|------|------|------|------|-----|
| 표지 헤드라인 | 56px | 800 | -.038em | `--ink` (강조어 `--forest`) |
| 슬라이드 제목 `h1.t` | 36px | 800 | -.031em | `--ink` (강조어 `--forest`) |
| eyebrow | 11px | 800 | .15em | `--gold` |
| 리드문 `.lede` | 15.5px | 400 | -.015em | `--ink-2` (강조어 `--forest` — 제목 계열) |
| 카드 타이틀 | 15~15.5px | 800 | -.025em | `--ink` |
| 본문 | 12.2~13px | 400 | -.012em | `--ink-2` |
| 라벨·캡션 | 10~11px | 800/600 | .1~.13em | `--ink-3` |

**한글 조판:** `word-break: keep-all` + `text-wrap: pretty` 전역 적용. 어절 중간에서 끊지 않는다.

### 강조 3단 (원칙 4의 구체형)

| 층위 | 색 | 쓰는 곳 |
|------|-----|---------|
| 헤딩 | `--forest` | `h1.t em`, `.lede strong`, 표지 헤드라인 |
| 본문 기본 | `--ink` 굵게 | `ul.list li b`, `.txt b`, `.pblk .pv b` |
| 특별 강조 | `--gold` | 기획 방향(`.mcell .v b`), 확인 지표(`.eff td.m`), 리워드 라벨 |
| 블루 면 위 | `--on-brand-accent` / `--on-brand` | `.quote p b`, `.formula`, `.nsm` |

---

## 4. 컴포넌트 규격

| 컴포넌트 | 규격 |
|----------|------|
| 카드 | `background:#fff` · `1px solid --line` · `radius 14px` · `--sh-1` |
| 블루 패널 | `background:--sage-2` · `1px solid rgba(27,95,217,.10)` · `radius 12px` |
| CTA 버튼 | `background:--forest` · 흰 글자 · `radius 8px` · 우측 chevron |
| 칩 (기본) | `--sage-2` 바탕 / `--forest` 글자 / `radius 999px` |
| 칩 (리워드) | `--gold-soft` 바탕 / `--gold` 글자 |
| 표 헤더 (결론형) | `--forest` 바탕 / 흰 글자 — 슬라이드의 결론을 담는 표(9장 기대효과) |
| 표 헤더 (참조형) | `--sage-2` 바탕 / `--forest` 글자 — 사실을 나열하는 표(5·7장) |
| 표 짝수행 | `rgba(228,239,252,.42)` |
| 강조 블록 | `--forest` 단색 또는 `--forest → --forest-2` 그라디언트 + 흰 글자 |
| 좌측 강조 보더 | `3px solid --gold-2` |
| 그림자 | `--sh-1` 카드 / `--sh-2` 떠 있는 요소 |

---

## 5. 슬라이드 골격

레퍼런스 4장의 공통 골격을 그대로 따른다.

```
┌──────────────────────────────────────────────┐
│ [챕터 레일 · 활성=블루 알약]        [브랜드마크] │  ← 상단 62px
├──────────────────────────────────────────────┤
│  ─ eyebrow (앰버)                             │
│  슬라이드 제목 (거의 검정 + 블루 강조어)          │
│  리드문 (짙은 슬레이트)                         │
│                                              │
│  [ 콘텐츠 ]                                    │
│                                              │
│  [ 하단 요약 스트립 — 블루 틴트 바 ]              │  ← 레퍼런스의 하단 바 대응
├──────────────────────────────────────────────┤
│ 태그라인                              05 / 12 │  ← 하단 52px
└──────────────────────────────────────────────┘
```

- 좌우 여백 56px, 상단 88px(레일 포함), 하단 68px.
- **표지·클로징만 예외** — 크롬을 숨기고 화이트 전면을 쓴다.

---

## 5-1. 앱 화면 목업 규칙

앱 화면은 **원본 이미지를 배경 크롭하지 않는다.** `background-position` 퍼센트로 폰을 맞추려 하면
좌표 오차로 프레임이 잘리거나 옆 화면·캡션이 새어 들어온다. 이 방식으로 세 번 실패했다.

### 폰 경계 측정 — 눈대중 금지

둥근 베젤 때문에 **"베젤이 최대 폭이 되는 행"만 찾으면 위아래 곡면이 잘린다.**
반드시 중앙 열까지 훑어 곡면의 끝을 찾는다.

1. PNG를 디코딩해 픽셀에 접근한다 (`zlib` + 스캔라인 언필터, 표준 라이브러리만으로 가능).
2. 임계값 `R+G+B < 330`(거의 검정)으로 베젤 픽셀만 잡는다 — 파란 화살표·본문 텍스트가 섞이지 않는다.
3. 좌·우 베젤이 **동시에** 있는 행 구간을 폰의 코어로 잡는다.
4. 코어 위아래로 확장한다 — **가로 중앙 ±40px에 어두운 픽셀이 있으면 아직 베젤 곡면**이다.
5. 좌우도 같은 방식으로 확장한다.

### 파이프라인

```bash
sips -c <h+12> <w+12> --cropOffset <y-6> <x-6> "<원본>" --out assets/ux-x.png   # 진짜 경계 + 6px 여백
sips --resampleHeight 690 assets/ux-x.png --out assets/ux-x.png                 # 폰 높이 통일
sips -p 690 380 --padColor FFFFFF assets/ux-x.png --out assets/ux-x.png         # 공통 캔버스 중앙 패딩
```

모든 파일이 **380×690** 단일 캔버스라 어느 박스에 넣어도 크기·위치가 자동으로 일치한다.

### 표시 박스 — 비율을 캔버스에 맞춘다

```css
.shot{ height:<원하는 높이>; aspect-ratio:380/690;
       background-size:contain; background-position:center; }
```

박스 비율이 캔버스와 같으면 레터박스가 0이므로 **잘림도 0, 남는 여백도 0**이다.
한 변만 지정하고 나머지는 `aspect-ratio`에 맡긴다. 박스 크기를 키우고 싶으면
주변 패딩·간격을 줄여 높이를 확보한다 — 비율은 절대 건드리지 않는다.

| 파일 | 원본 화면 | 사용처 | 표시 크기 |
|------|-----------|--------|-----------|
| `ux-landing.png` | 01 서비스 랜딩 | 4장 col1 · 6장 장면1 | 205×372 / 182×330 |
| `ux-home.png` | 02 오늘의 홈 | 4장 col2 | 205×372 |
| `ux-predict.png` | 04 예측 제출 | 5장 대표 · 6장 장면2 | 232×421 / 182×330 |
| `ux-result.png` | 06 결과 확인 | 6장 장면3 | 182×330 |
| `ux-ranking.png` | 07 주간 랭킹 | 4장 col3 | 205×372 |
| `ux-share.png` | 10 공유하기 | 6장 장면4 | 182×330 |

### 검증

빨간 배경 위에 `object-fit:contain`으로 렌더링해 **네 변 모두 빨강이 보이고
위아래 둥근 모서리가 온전한지** 확인한다. 슬라이드 렌더링에서도 폰 상·하단을
4배 확대해 곡면이 직선으로 잘리지 않았는지 본다.

---

## 6. 로고 사용 규칙

`makji-logo.png` — 블루 스크립트, 배경 투명.

| 위치 | 크기 | 규칙 |
|------|------|------|
| 표지 | 236px | 옅은 블루 틴트 플레이트 위 |
| 3장 순환 루프 중앙 | 82px | 블루 틴트 면 위 |
| 클로징 | 190px | 화이트 위 직접 배치 |

- **금지:** 어두운 면 위 직접 배치(대비 부족), 밝기 필터로 색 왜곡, 종횡비 변경, 88px 미만 축소(아치형 영문이 뭉갬).
- 반복 사용은 위 3곳까지. 그 외 슬라이드에서는 텍스트 브랜드마크(`MAKJI 브레드마켓`)를 쓴다.

---

## 7. 뷰포트 · 스케일링

- 무대는 **1440×810 고정**. `transform: scale()`로 뷰포트에 맞춘다.
- `scale = min(vw/1440, vh/810)` — 잘리는 영역이 없어야 한다.
- **가로가 부족할 때(`vw/1440 < vh/810`)는 무대를 화면 왼쪽에 붙인다** (`transform-origin:left center` + `justify-content:flex-start`).
- 가로가 충분할 때는 가운데 정렬.
- 무대 바깥 여백은 짙은 네이비 — 화이트 무대가 떠 보이게 한다.

---

## 8. 접근성 · 견고성

- 명도 대비: 본문 `--ink-2`(#42525F) 8.0:1 · 라벨 `--ink-3`(#64748B) 4.8:1 · CTA 흰 글자 on `--forest` 5.7:1 — 모두 WCAG AA(4.5:1) 충족.
- `--ink-3`는 한때 `#7C8CA0`(3.4:1)이었고 AA 미달이었다. **밝은 회색으로 되돌리지 말 것.**
- `prefers-reduced-motion` 존중 — 등장 애니메이션 제거.
- 모든 의미 있는 이미지에 `alt` / `aria-label`.
- `@media print` 로 슬라이드 1장 = 1페이지 PDF 내보내기 지원.

---

## 9. 체크리스트 (슬라이드 추가·수정 시)

- [ ] 새 색을 하드코딩하지 않고 토큰을 썼는가
- [ ] 설명 문구가 `--ink-3`이 아니라 `--ink-2`인가
- [ ] 강조가 **3단 표**의 층위에 맞는가 (헤딩=블루 / 본문=잉크 / 특별=앰버 / 블루 면 위=on-brand)
- [ ] 1440×810 안에서 잘리거나 넘치는 요소가 없는가
- [ ] 한글이 어절 중간에서 끊기지 않는가
- [ ] 앱 화면을 배경 크롭이 아니라 `assets/ux-*.png` 정규화 파일로 넣었는가
- [ ] 헤드리스 렌더링으로 **실제 화면을 눈으로** 확인했는가

```bash
CH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
for n in $(seq 1 12); do
  "$CH" --headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=1 \
    --window-size=1440,810 --virtual-time-budget=2500 \
    --screenshot="shots/s$(printf '%02d' $n).png" "file://$PWD/index.html#$n"
done
```
